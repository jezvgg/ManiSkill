- Retry the failure-only west-pot transport candidate after gangway reconnects: add `x_min=0.35` to `_velocity_segment`, pass `x_min=0.05` only from FridgeVeggies `_transport_veg`. Stable seed77 ends at base x=0.307 with a safe ideal target x=0.130, so the current x<0.35 guard aborts before motion. Run #154 was unjudged because the eternal job disconnected during the batch and no logs were pulled.
